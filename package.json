{
  "name": "chat",
  "version": "1.0.0",
  "description": "2. `npm install http-server -g` to get [simple HTTP file server](https://github.com/nodeapps/http-server),\r  you may want to use Apache or any other\r 3. `http-server . -p 8010`\r 4. Open `http://localhost:8010/` to check if you see home page",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.4.0"
  }
}
